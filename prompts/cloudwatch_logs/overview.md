# CloudWatch Log Analysis

This capability is reserved for CloudWatch log triage inside the enclave.

It should be used when the artifact set is built from CloudWatch log exports, log group snapshots, or other CloudWatch-derived operational evidence.

This folder is intentionally dataset-specific. CloudWatch prompts should be tuned to the exact exported fields, timestamp shape, severity markers, and service context in the log bundle being analyzed.
