# Bedrock Orchestration Layer

This directory is reserved for orchestration artifacts:

- Prompt Management promotion targets
- Flow JSON definitions
- Lambda transform contracts
- Step Functions hook points

Per ADR 001, this layer governs execution and lifecycle but does not embed reasoning heuristics from Ask EIDS.
