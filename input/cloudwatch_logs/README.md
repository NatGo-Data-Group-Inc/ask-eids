# CloudWatch Artifact Drop Location

Place CloudWatch log exports or derived CloudWatch log bundles here.

This directory is the default input location for the `cloudwatch_logs` capability scaffold.

Current status:

- The shared capability framework can target `cloudwatch_logs`.
- The CloudWatch analyzer itself is still a scaffold and needs dataset-specific extraction logic before operational use.
